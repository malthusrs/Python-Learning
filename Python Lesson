#exibe a lista de numeros primos em um intervalo
ponto_final = int(input('INsira o numero final do intervalo  -> '))
cont =1
lista=[]
while cont<=ponto_final:
    contagem = 0
    for i in range(1, cont+1,1):
        if cont%i==0:
            contagem+=1
    if contagem<=2:
        lista.append(cont)

    cont+=1

print('Os números primo
